sudo: required

language: python

services:
  - docker

python:
  - "2.7"

notifications:
  email: false

before_install:
  - docker pull tfarmer/mmtk_dependencies
  - docker run tfarmer/mmtk_dependencies

script:
  - pytest -vv

branches:
  only:
    - master
