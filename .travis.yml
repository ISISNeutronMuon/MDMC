sudo: required

language: python

services:
  - docker

python:
  - "2.7"

notifications:
  email: false

before_install:
  - docker pull tfarmer/mmtk_dependencies
  - docker run mmtk_dependencies

script:
  - pytest -vv directory

branches:
  only:
    - master
