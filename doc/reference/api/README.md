# API Reference
Yes, this directory is empty! When the documentation is built for the website,
the API is built using sphinx-apidoc. To build it yourself, call

```
sphinx-apidoc ./MDMC -o ./doc/reference/api/
```

from the main repository folder.
