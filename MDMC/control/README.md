# control
A package for controlling MDMC.  It is responsible for the interactions between the packages which run the molecular dynamics simulation, calculate the experimental observables and the implement force field parameter refinement algorithms.
