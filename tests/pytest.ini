[pytest]
markers =
    lammps: marks tests that use lammps
    dlpoly: marks tests that use dlpoly
    openmm: marks tests that use openmm
    mpi: marks tests that are in mpi testing

addopts = --cov="$(pwd)"/MDMC --cov-report xml --profile
