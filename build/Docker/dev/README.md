This folder contains a docker-compose file which loads your local copy of MDMC into a Docker container. This is useful for development. Simply place this in the same directory containing the MDMC source code, and run `docker-compose pull` and `docker-compose up` in that directory to spin up a Docker container running your local MDMC code.

Requires Docker and docker-compose to be installed, naturally.
